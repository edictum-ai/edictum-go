apiVersion: edictum/v1
kind: ContractBundle
metadata:
  name: security-command-allowlist
defaults:
  mode: enforce
contracts:
  - id: allow-echo
    type: sandbox
    tool: Bash
    allows:
      commands:
        - echo
    message: "Only echo is allowed"
