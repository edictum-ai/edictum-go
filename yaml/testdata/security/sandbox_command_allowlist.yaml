apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: security-command-allowlist
defaults:
  mode: enforce
rules:
  - id: allow-echo
    type: sandbox
    tool: Bash
    allows:
      commands:
        - echo
    message: "Only echo is allowed"
