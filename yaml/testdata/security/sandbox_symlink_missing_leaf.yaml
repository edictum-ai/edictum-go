apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: security-symlink-leaf
defaults:
  mode: enforce
rules:
  - id: within-link
    type: sandbox
    tool: ReadFile
    within:
      - __LINK_ROOT__
    message: "Read must stay inside linked root"
