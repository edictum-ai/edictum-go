apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: invalid-mode
defaults:
  mode: shadow
rules:
  - id: c1
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
