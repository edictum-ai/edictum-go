apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: missing-contract-id
defaults:
  mode: enforce
rules:
  - type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
