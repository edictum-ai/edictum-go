apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: bad-tools
defaults:
  mode: enforce
tools:
  Bash:
    side_effect: destructive
rules:
  - id: c1
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
