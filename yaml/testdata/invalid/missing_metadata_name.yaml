apiVersion: edictum/v1
kind: Ruleset
metadata:
  description: missing-name
defaults:
  mode: enforce
rules:
  - id: c1
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
