apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: extra-top-level
defaults:
  mode: enforce
rules:
  - id: c1
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
extra: true
