apiVersion: edictum/v1
kind: ContractBundle
metadata:
  name: extra-top-level
defaults:
  mode: enforce
contracts:
  - id: c1
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      effect: deny
      message: "Denied"
extra: true
