kind: Ruleset
metadata:
  name: missing-api
defaults:
  mode: enforce
rules:
  - id: c1
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
