apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: missing-tool
defaults:
  mode: enforce
rules:
  - id: c1
    type: pre
    when:
      "args.command":
        contains: "rm"
    then:
      action: block
      message: "Denied"
