apiVersion: edictum/v1
kind: ContractBundle
metadata:
  name: bad-post-effect
defaults:
  mode: enforce
contracts:
  - id: c1
    type: post
    tool: Bash
    when:
      "output.text":
        contains: "secret"
    then:
      effect: approve
      message: "Denied"
