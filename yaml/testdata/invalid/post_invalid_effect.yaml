apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: bad-post-effect
defaults:
  mode: enforce
rules:
  - id: c1
    type: post
    tool: Bash
    when:
      "output.text":
        contains: "secret"
    then:
      action: ask
      message: "Denied"
