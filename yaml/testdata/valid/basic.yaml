apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: basic-bundle
defaults:
  mode: enforce
rules:
  - id: no-rm
    type: pre
    tool: Bash
    when:
      "args.command":
        contains: "rm -rf"
    then:
      action: block
      message: "Cannot run rm -rf"
